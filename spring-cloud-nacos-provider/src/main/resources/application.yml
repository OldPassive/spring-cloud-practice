server:
  port: 9001

spring:
  application:
    name: cloud-service-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
